[project]
name = "improbable-adventure"
version = "0.1.0"
description = "dynamic dna tokenization and pre-training"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=4.4.2",
    "genomic-benchmarks>=1.0.0",
    "matplotlib>=3.10.8",
    "torch>=2.9.1",
]
